import React from 'react';
import { Building } from 'interfaces/models/game/building';

type BuildingCardProps = {
  buildingId: Building['id'];
}

const BuildingCard: React.FC<BuildingCardProps> = ({ buildingId }) => {

  return (
    <></>
  )
};
